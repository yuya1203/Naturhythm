<% unless current_micropost.favorite?(micropost) %>
  <%= form_for(current_micropost.favorites.build) do |f| %>
    <%= hidden_field_tag :micropost_id, micropost.id %>
    <%= f.submit 'お気に入り', class: 'btn btn-primary btn-block' %>
  <% end %>
<% end %>